# 🧮 TimesTableGen

A simple C++ program that generates and displays the multiplication table of a given number from 1 to 10.

## 🧩 Purpose

This program allows users to quickly view the multiplication table for any number between 1 and 10.  
It includes basic input validation to ensure the entered value is numeric and within the valid range.

## 🚀 How to Run

### Using an IDE (e.g., Visual Studio, Code::Blocks)
1. Create a new C++ file named `TimesTableGen.cpp`.
2. Paste the program code into it.
3. Compile and run the program.
4. Enter a number between 1 and 10 to view its multiplication table.

### Using Command Line (Windows)
1. Open a terminal/PowerShell window.
2. Navigate to the folder containing `TimesTableGen.cpp`.
3. Compile the program using a C++ compiler (e.g., g++):
   ```bash
   g++ TimesTableGen.cpp -o TimesTableGen.exe
