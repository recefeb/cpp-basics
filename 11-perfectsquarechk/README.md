# 🧮 PerfectSquareCheck

A simple C++ program that determines whether a given positive number is a perfect square.

## 🧩 Purpose

This program checks if the entered number has an integer square root — for example, 4, 9, and 16 are perfect squares.  
It also rejects invalid inputs such as negative or non-numeric values.

## 🚀 How to Run

### Using an IDE (e.g., Visual Studio, Code::Blocks)
1. Create a new C++ file named `PerfectSquareCheck.cpp`.
2. Paste the program code into it.
3. Compile and run the program.
4. Enter a positive number when prompted.
5. The program will determine whether the number is a perfect square.

### Using Command Line (Windows)
1. Open a terminal/PowerShell window.
2. Navigate to the folder containing `PerfectSquareCheck.cpp`.
3. Compile the program using a C++ compiler (e.g., g++):
   ```bash
   g++ PerfectSquareCheck.cpp -o PerfectSquareCheck.exe
