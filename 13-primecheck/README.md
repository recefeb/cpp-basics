# 🧮 PrimeCheck

A simple C++ program that determines whether a given positive integer is a prime number.

## 🧩 Purpose

The program checks if the entered number is prime by testing divisibility from 2 up to the integer square root of the number.  
It rejects invalid or non-positive inputs and efficiently identifies prime numbers.

## 🚀 How to Run

### Using an IDE (e.g., Visual Studio, Code::Blocks)
1. Create a new C++ file named `PrimeCheck.cpp`.
2. Paste the program code into it.
3. Compile and run the program.
4. Enter a positive integer when prompted.

### Using Command Line (Windows)
1. Open a terminal/PowerShell window.
2. Navigate to the folder containing `PrimeCheck.cpp`.
3. Compile the program using a C++ compiler (e.g., g++):
   ```bash
   g++ PrimeCheck.cpp -o PrimeCheck.exe
